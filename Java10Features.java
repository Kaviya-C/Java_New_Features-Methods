package com.java8.finishing;

public class Java10Features {
	int c = 100;

	public static void main(String... args) {
		var i = 10;
		var a = "Name";
		var b = 10.00;

	}
	/*
	 * var-- keyword from 10 onwards
	 * 
	 * declare only local variable without their types compiler automatically
	 * detects the type based on the intializer automatic type inference
	 * 
	 * java 10 var keyword java 10 local variable type inference
	 * 
	 * important point: var work only when u intitalize the variable explicitly var
	 * only for local variable not for global variables var cannot be used as method
	 * parameter or return type var does not work with lambda expression -->
	 * explicit target type we can modify the var value but the value should be of
	 * same type we can't initialize the variable with null var remove unneccssary
	 * repetetion of the code on the left side of the declaration statement var is
	 * not keyword ,reserved type name like int float etc var as variable name
	 * method name ,class name
	 * 
	 * 
	 */

}
